generated: '2026-08-09'
method: generated
source: https://app.1lookup.io/api + https://www.1lookup.io/products/mcp + repo artifacts
note: >-
  1Lookup publishes no OpenAPI, so these skills are grounded in the HTTP method + path pairs printed
  verbatim in the provider's API reference and the five tool names published on the MCP product page.
  No operation, parameter or field below was invented; where a request field is not published, the skill
  says so instead of guessing.
skills:
  - file: 1lookup-validate-contact-before-outreach.md
    name: Validate a contact before you dial, text or email it
    api: https://app.1lookup.io/api/v1
    operations: ['POST /api/v1/phone', 'POST /api/v1/email', 'POST /api/v1/ip', 'GET /api/v1/account']
    mcp_tools: [validate_phone, verify_email, ip_lookup, get_account]
  - file: 1lookup-bulk-list-hygiene.md
    name: Clean a whole contact list with a bulk job
    api: https://app.1lookup.io/api/v1
    operations: ['POST /api/v1/bulk/jobs', 'GET /api/v1/bulk/jobs/{job_id}', 'GET /api/v1/bulk/jobs/{job_id}/results', 'GET /api/v1/account']
    mcp_tools: [bulk_verify, get_account]
  - file: 1lookup-enrich-company-and-contact.md
    name: Enrich a company domain into firmographics and reachable contacts
    api: https://app.1lookup.io/api/v1
    operations: ['POST /api/v1/company-firmographics', 'POST /api/v1/prospect-search', 'POST /api/v1/b2b-contact-append', 'POST /api/v1/account-search', 'GET /api/v1/account']
    mcp_tools: []
